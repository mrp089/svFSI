name: test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build without trilinos
        shell: bash
        run: |
          sudo apt-get install -y libmpi libmpi_cxx
          mkdir build
          cd build
          cmake ..
          make
